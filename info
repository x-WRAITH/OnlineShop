dodac do kazdego mode zabezpiecznie przed wbiciem ;)
oczyscic mode 
if($_GET['submode']=='id, producerName'){
    $q="SELECT * FROM producer WHERE id='{$_GET['id']}', producerName='{$_GET['producerName']}'";
    }} else {
    $q="SELECT * FROM producer";
    }

    nie potrzebne $q gdzie jest id i producername np wskazywane skoro ma wyswietlac wszystko XD
    zrobic to na podstawie mode/admin/products.php ;)